{% extends 'base.html.twig' %}
{% block title %}
    {{ 'nom_du_site' | trans }} - {{ 'profile_mofication_title' | trans }}
{% endblock %}
{% block body %}
    <div class="container">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">{{ 'update' | trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}


