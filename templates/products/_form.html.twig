<div class="container">
    <h1>{{ titleText | trans }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn btn-{{ type|default('primary') }}">{{ btnText | trans }}</button>
    {{ form_end(form) }}
</div>
